import {
  
} from './actions'

/**
 *  Class to encapsulate all functions and method to validate a Form
 */
export default class FormFieldsValidator {

    constructor(){
        
    }

    /**
     * Check all layout to validate current stage
     * @param {*} layout 
     */
    validateFields(layout,currentStage) {

    }
    
}
